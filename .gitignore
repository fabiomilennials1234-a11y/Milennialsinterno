# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.env
.env.local
.env.scripts
node_modules
dist
dist-ssr
*.local

# Cursor / IDE
.cursor

# Claude Code local config (per-user, never commit)
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local scratch area (screenshots, debugging artifacts) — never commit
_scratch/

# Root-level screenshots from macOS (accidentally saved here)
/Captura de Tela*.png
/Screen Shot*.png
/Screenshot*.png

# Large binaries don't belong in git — use external storage
*.pdf
*.zip
*.mp4

# Playwright MCP
.playwright-mcp/

# Coverage reports
coverage/
